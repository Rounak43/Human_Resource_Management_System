# Server Network Settings
PORT=5000
NODE_ENV=development

# Database Settings (PostgreSQL Connection Details)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=hrms_db
DB_USER=hrms_user
DB_PASSWORD=hrms_password_secure

# Token Auth Settings (JWT)
JWT_SECRET=super_secret_jwt_sign_key_change_me_in_production
JWT_EXPIRES_IN=24h
